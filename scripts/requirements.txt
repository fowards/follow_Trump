# 데이터 파이프라인 의존성
pypdf>=4.0           # PDF 텍스트 레이어 추출
pypdfium2>=4.0       # 스캔 PDF → 이미지 렌더링 (휠 지원 범위가 넓어 기본 선택)
pytesseract>=0.3.10  # Tesseract 연결 (Tesseract 본체는 별도 설치)
pillow>=10.0         # 이미지 처리

# 선택: pymupdf 가 있으면 렌더링이 더 빠릅니다. 없으면 pypdfium2를 씁니다.
# 파이썬이 너무 최신이면 휠이 없어 설치가 실패할 수 있으니 필수로 두지 않습니다.
#   pip install pymupdf
